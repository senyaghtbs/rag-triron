apiVersion: v2
name: rag-system
description: A RAG system with Qdrant and Embedder
type: application
version: 0.1.0
appVersion: "1.0"

dependencies:
  - name: qdrant
    version: "0.9.0"
    repository: "https://qdrant.github.io/qdrant-helm"
    condition: qdrant.enabled